<Window x:Class="ReflectionChessApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Reflection Chess App"
        Height="750"
        Width="950"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12">

            <StackPanel>
                <TextBlock Text="Загрузка библиотеки классов"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="0,0,0,10"/>

                <TextBlock Text="Путь к DLL:" FontWeight="Bold"/>

                <DockPanel Margin="0,5,0,0">
                    <TextBox Text="{Binding LibraryPath, UpdateSourceTrigger=PropertyChanged}"
                             Height="30"
                             VerticalContentAlignment="Center"
                             DockPanel.Dock="Left"/>

                    <Button Content="Загрузить библиотеку"
                            Width="170"
                            Margin="10,0,0,0"
                            Command="{Binding LoadLibraryCommand}"/>
                </DockPanel>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12"
                Margin="0,15,0,0">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Найденные классы"
                               FontSize="16"
                               FontWeight="Bold"
                               Margin="0,0,0,8"/>

                    <ComboBox ItemsSource="{Binding Classes}"
                              SelectedItem="{Binding SelectedClass}"
                              DisplayMemberPath="Name"
                              Height="30"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                    <TextBlock Text="Методы выбранного класса"
                               FontSize="16"
                               FontWeight="Bold"
                               Margin="0,0,0,8"/>

                    <ComboBox ItemsSource="{Binding Methods}"
                              SelectedItem="{Binding SelectedMethod}"
                              DisplayMemberPath="DisplayName"
                              Height="30"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12"
                Margin="0,15,0,0">

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <GroupBox Header="Параметры конструктора" Grid.Column="0" Margin="0,0,10,0">
                    <ItemsControl ItemsSource="{Binding ConstructorParameters}" Margin="10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="0,4,0,4">
                                    <TextBlock Width="160"
                                               VerticalAlignment="Center"
                                               Text="{Binding Name}"/>

                                    <TextBlock Width="80"
                                               VerticalAlignment="Center"
                                               Text="{Binding TypeName}"/>

                                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                             Height="26"/>
                                </DockPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>

                <GroupBox Header="Параметры метода" Grid.Column="1" Margin="10,0,0,0">
                    <ItemsControl ItemsSource="{Binding MethodParameters}" Margin="10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <DockPanel Margin="0,4,0,4">
                                    <TextBlock Width="160"
                                               VerticalAlignment="Center"
                                               Text="{Binding Name}"/>

                                    <TextBlock Width="80"
                                               VerticalAlignment="Center"
                                               Text="{Binding TypeName}"/>

                                    <TextBox Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                             Height="26"/>
                                </DockPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </GroupBox>
            </Grid>
        </Border>

        <Border Grid.Row="3"
                BorderBrush="Gray"
                BorderThickness="1"
                CornerRadius="6"
                Padding="12"
                Margin="0,15,0,0">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Button Content="Выполнить"
                        Width="140"
                        Height="35"
                        HorizontalAlignment="Left"
                        Command="{Binding ExecuteMethodCommand}"/>

                <TextBox Grid.Row="1"
                         Text="{Binding Result}"
                         Margin="0,15,0,0"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         AcceptsReturn="True"/>
            </Grid>
        </Border>
    </Grid>
</Window>